# CPU-friendly install for demos and CI.
# For CUDA, install a matching torch/torchvision wheel from https://pytorch.org first.
torch>=2.1
torchvision>=0.16
numpy>=1.24
Pillow>=10.0
PyYAML>=6.0
tqdm>=4.66
